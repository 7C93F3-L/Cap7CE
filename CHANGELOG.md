# Cap7CE Changelog

## 0.9.9

- 主窗口现在分别记忆左侧栏与 Skim 右侧栏的用户调整宽度；记录仅保存在 Renderer 本地并经过合法范围校验与短防抖写入，不新增偏好 IPC。窄窗口仍按既有响应布局临时限幅，重新拉宽或重启后恢复用户原始宽度；键盘调整和双击恢复默认同样会更新记录，损坏或缺失数据安全回退 176px / 360px。

- 修正新版 Skim 加载与空目录提示仍沿用旧暗色弱化文字、在黑暗亚克力中对比不足的问题；两类正文现在仅在 Skim 内容区使用新版主题弱化文字色，明暗亚克力与云母会分别适配，不改变搜索结果、路径状态、侧栏或弹窗文字。

- 恢复新版 Skim 区域的鼠标侧键导航，并将顶部返回与侧键前进 / 后退统一为按实际访问顺序维护的浏览历史：从“此电脑”直接打开文档等系统位置后可一步返回 Skim 根目录，随后仍可前进恢复；只有成功读取的位置才会进入历史，双击、快速切换和被取消的迟到请求不会污染记录。到达最早记录后继续后退保持内容和面板不变；搜索结果区和左侧栏仍沿用主窗口导航，不新增全局钩子或 IPC。

- 主窗口搜索框在存在关键词时新增圆形清空按钮，使用接近搜索框表面的自适应浅底色与独立高对比叉号，并略微放大、向右贴近边缘；点击后复用既有空查询刷新语义并将焦点留在输入框，当搜索框实际宽度低于 180px 时自动隐藏按钮，优先保留文字输入空间。

- 主窗口已添加目录的悬停与选中背景改由整行容器绘制，将管理三点按钮包含在与“全部目录”一致的胶囊范围内；三点按钮使用略向内收的圆形悬停背景，并单独增强暗色亚克力下的对比度，收起边栏尺寸与交互保持不变。

- 优化搜索结果分类卡片的文字层级：标题不再使用主题“颜色 1”，改为普通主文字色和稳定字重，说明文字按主文字统一弱化；移除 AI 匹配进行中标题与说明的呼吸变色动效，保留实时进度文案、暂停 / 继续操作及卡片交互。

- 修复云母材质下 Preview 边栏“嵌入信息”有内容时遗漏卡片描边的问题；内容态与空状态现在共用同一套亮色 / 暗色云母描边变量，其他材质不变。

- PDF 及转换为 PDF 的 PPT / Excel Preview 改为默认适合内容区宽度并移除页面圆角，新增 75%–200% 文档缩放：顶部提供缩小、百分比复位和放大控件，同时支持 Ctrl+滚轮与右键上下拖动；缩放按当前页面锚点补偿横纵滚动位置，放大后可按住左键拖动画面并启用横向滚动条，连续页面懒加载、页码、主进程 1600px 渲染上限、缓存及 Office 转换链保持不变。

- 图片 Preview 新增按住鼠标右键上下拖动的连续缩放：以按下位置为固定缩放中心，向上放大、向下缩小，约 200px 对应两倍变化并沿用现有 1–6 倍限制；指针捕获保证越界松键仍能结束，缩至 1 倍自动归正，现有 Ctrl+滚轮缩放、左键平移、双击复位和窗口尺寸链保持不变。

- Preview 边栏“当前匹配依据”不再重复罗列本次查询词，改为按每个查询词的现有最佳命中来源分组显示“来源 + 关键词”，可区分文件名、文件夹、嵌入信息、手动元数据、自然条件和 AI 来源；同一来源的多个词自动合并，Skim 无搜索上下文时仍显示原空状态，不改搜索、排序与索引逻辑。

- 精简 Preview 边栏“嵌入信息”中的画面描述布局：隐藏与区块标题重复的“画面内容”字段名，让正文独占卡片宽度并正常换行；标题、主题、设备等其他嵌入字段继续保留标签和双列结构。

- 调整 Preview 边栏中的文件名与完整路径排版：取消文件名的单行省略并允许连续长字符在卡片内换行，路径采用相同的防溢出规则；每项常规内容自然展开，超过六行后在文字区域内部滚动，保留完整内容而不让极端长名称或路径无限撑高边栏。

- 修复暗色亚克力 Preview 边栏卡片仍硬编码使用亮色半透明背景、造成浅色文字与卡片对比度异常的问题；边栏卡片现在复用 Preview 内容卡片的主题表面变量，亮暗亚克力会正确切换，云母现有高不透明度覆盖保持不变。

- 提高亚克力材质下主窗口与 Settings 的边栏搜索框、导航悬停/选中状态，以及 Skim 路径搜索框的表面不透明度：明亮模式统一为 68% 白色，暗色模式统一为 42% 黑色，使交互区域和背景形成更清晰的层级；云母保持原样。

- 调整主窗口纵向文件右键菜单的操作分组间距：在“打开路径”与分隔线之间补齐和分隔线至“复制路径”相同的 5px 留白，低高度下的横向紧凑菜单保持原布局。

- 修正主窗口左侧栏折叠时排序图标的 30px 最小内容宽度撑开 24px 网格内容区、产生视觉错位的问题；折叠态网格列现在允许收缩并让图标向两侧对称溢出，保留原尺寸，展开侧栏、Skim 工具栏与原始图标资源保持不变。

- 修复主窗口冷启动时首次“全部目录”搜索早于内容视图激活确认、导致外部新增或更新文件只在切换目录后出现的问题；首次搜索现在等待主进程确认前台内容活动后只执行一次，失焦与隐藏仍立即取消扫描，不恢复启动期索引写入或绕过后台节流。

- 新版 Settings【快捷动作】总开关改用与其他布尔设置一致的滑块开关样式，保留启用状态文案、快捷键录入期间禁用以及原有注册行为。

- 修复独立 Settings 的诊断与版本更新动作仍被主窗口专属权限拒绝的问题：日志和崩溃报告位置、详细记录、诊断包导出、更新检查、下载与取消现在认可 Settings 发送者；诊断保存对话框跟随实际发起窗口，更新进度直接返回 Settings，未知 Renderer 继续被拒绝。

- 修复独立 Settings 中“嵌入元数据”检查按钮仍被主窗口专属 IPC 权限拒绝的问题；显式补齐、取消和任务状态现在由主窗口与 Settings 共用同一服务并同步广播，按钮左侧显示正在检查、处理进度、完成或失败反馈。

- 窗口宿主完成 stable-only 收口：主窗口、独立 Settings 与 Preview 统一由 40 DIP WCO 和同一套 Acrylic / Mica 运行时创建，移除旧 Cap7CE / compatibility 外壳策略、模式切换事务、兼容最大化控制器、旧设置回退入口及相关 IPC / preload / 偏好字段；主窗口状态只保留 normal / standby，最大化与分屏回归 Windows 原生语义。窗口布局固定使用 `window-layout-stable-ui.json` 并默认记忆，历史偏好 JSON 的多余字段和旧布局文件保留在磁盘但不再读取或改写。

- 完成 stable-only 最终源码审计：删除旧搜索胶囊、旧 Results / Skim 右键菜单适配层、旧分栏菜单、占位网格、专用 CSS、零调用图标与旧快捷动作运行时名称；搜索结果和 Skim 统一只保留响应式单层文件菜单。快捷键偏好改用 `focusMainSearch`、`hideToLine`、`toggleSkim` 与 `restoreDefaultWindow` 等当前语义，旧键名只在读取已有配置时映射，micro / mini 字段被安全忽略。架构守门同步收紧到现有 stable 入口；Electron 编译前清理限定范围内的旧输出与增量缓存，完整生产构建自动审计旧宿主标识，避免已删除模块残留进后续产物。

> 本节其余条目按 0.9.9 开发过程保留；其中“旧宿主保持不变”“旧回退链仍保留”等表述只描述当时阶段，最终产品状态以上述 stable-only 收口为准。

- 新版 stable 主窗口边栏弹出菜单的已选项改用亮色纯白、暗色纯黑的不透明中性背景，避免格式及功能图标使用主题色时与选中底色相互弱化；菜单悬停与边栏目录状态保持不变。

- 新版 stable 主窗口与独立 Settings 的导航悬停和选中状态复用同一语义表面：浅色模式统一为白色 50%，暗色模式统一为黑色 24% 压暗层；主窗口目录、顶部控制、底部按钮、重命名输入及设置分类导航保持一致，其他设置控件的悬停样式不变。云母材质额外启用更高不透明度的内容、卡片、菜单与交互表面，并以不影响布局的内描边分隔层级；亚克力维持原有通透度，主窗口、Settings 与稳定 Preview 会随材质偏好同步切换对比度方案。

- 新版 stable 主窗口与独立 Settings 的搜索框直接复用边栏选项的同一状态表面，浅色模式统一为白色 50%、暗色模式统一为黑色 24%；共享令牌同时覆盖独立挂载的主窗口标题栏 Portal，各自尺寸、输入与焦点行为保持不变。

- 新版 stable 主窗口与独立 Settings 的搜索框弱提示文字改用同一语义颜色，并显式移除浏览器默认透明度差异；浅色模式为深色文字 54%，暗色模式为浅色文字 48%。

- 新版 stable Skim 网格的选中状态不再沿用旧版灰色面板或主题色透明层，改用 68% 的中性半透明表面，让格式图标自身的主题色保持清晰，并与普通 30%、悬停 50% 建立连续层级；旧版 Skim 保持不变。

- 新版 stable 搜索结果卡片改用与 Skim 普通网格一致的柔和半透明表面，浅色模式为白色 30%；Skim 网格悬停和键盘焦点改用更清晰的白色 50% 表面。深色模式继续使用对应的深色半透明层。

- 新版 stable 的文本与 Markdown、PDF、压缩包、字体、EPUB / MOBI、音频和视频预览统一适配半透明卡片、圆角层级与中性次要文字；PDF 纸张和视频画布保留内容所需的原生底色。音频预览新增现有格式图标与文件摘要，继续使用原生播放器，旧预览宿主及各格式读取、翻页、滚动和播放逻辑保持不变。

- 新版 stable 的通用文件与文件夹信息预览保留无需展开边栏即可读取的基础详情，并加入现有格式图标；内容区改用新版半透明卡片、中性字段颜色与响应式窄窗排版。普通文件、扩展失败信息和文件夹分别使用与固定摘要结构匹配的内容尺寸，窗口一次成形；长路径或详情超出可用空间时在面板内部滚动而不触发多文件预览切换。旧预览宿主尺寸与样式保持不变。

- 新版 stable 左侧栏的目录列表改为按内容高度增长并在超出可用空间后内部滚动；列表与底部操作之间的剩余空白归还给原生窗口拖动区域，目录项、输入框与自绘滚动条继续保持可交互的非拖动命中区。

### 新增

- 新版 stable Settings 的“窗口材质”由只读说明改为“外观”分类中位于主题切换上方的可持久化亚克力 / 云母选择；切换后无需重启即可同步刷新主窗口、独立 Settings 与 Preview，旧 Cap7CE / compatibility 宿主保持原有材质策略。所选材质在系统或 Electron 不支持时仍自动回退为当前主题的安全纯色，纯色不作为单独的用户选项。
- 新版稳定 UI Preview 的手动关键词改为边栏内直接编辑：现有关键词以可移除的小标签显示，输入框支持中文输入法并可按 Enter 添加一个或多个关键词，保存与取消均留在当前 Preview；保存继续复用正式关键词规范化和索引写入，并刷新主窗口当前搜索结果，旧版浮动关键词卡片及多选增删语义保持不变。
- 窗口宿主扩展为新版稳定界面、旧 Cap7CE 透明自绘模式和 Windows 原生按钮兼容模式：缺失或非法偏好默认进入新版稳定界面，已有明确旧模式偏好继续原样生效；三种模式可沿用受控重启入口依次切换并分别保存布局，新版主窗口、Settings 与 Preview 在开发和打包加载中使用同一正式 Renderer 路由，旧外壳和回退链不删除。
- 运行诊断新增实际启动窗口模式、受控模式切换结果及主窗口、预览、line、Capsule、启动提示的原生窗口创建失败记录；仅记录窗口类别、模式、切换状态和脱敏异常，不记录搜索内容或用户路径。

### 优化

- 新版 stable 主窗口接入既有“目录切换”快捷动作；默认 `Alt+Q`，并继续读取用户自定义快捷键，按“全部目录”及已添加目录的现有顺序逐项切换、末尾回绕，同时复用正式搜索刷新与切换提示链路。旧版行为不变。
- 新版 stable 主窗口接入既有“进入 Skim”快捷动作；默认 `Alt+5`，并继续读取用户自定义快捷键，在同一窗口中依次展开或收起 Skim 边栏，复用侧栏底部按钮的唯一开关状态与正式 Skim 读取链路。旧版继续保持进入 Skim 页面语义。
- 新版 stable 将全局窗口快捷动作收口为五项独立入口：`Alt+反引号` 显示并聚焦主窗口，`Alt+1` 安全关闭主窗口并显示 line，`Alt+2` 展开或收起 Skim，`Alt+3` 打开独立 Settings，`Alt+4` 以当前显示器工作区 82% 且不超过 1600×1000 的默认大小居中显示主窗口；目录循环仍使用 `Alt+Q`。新版不再注册或显示 micro / mini，也不保存横向或纵向尺寸预设；stable 与旧宿主分别保存快捷键配置，旧宿主的七项形态动作、默认键位和行为保持不变。
- 新版 stable Settings 将“运行信息”分类整理为“诊断”：移除页面、卡片与内容中的重复标题，只保留应用日志、崩溃报告、详细记录和诊断包四行紧凑操作；llama.cpp 文件、服务、运行目录与模型状态移动到“搜索与 AI”，紧跟对应运行时和模型设置，并以宽屏双列、窄屏单列的信息表展示。滚动区内建立最大 1000px 的统一响应式内容轨道，宽窗口中于右侧内容区居中，普通分组与自定义查看均占满同一轨道；共享自绘滚动框为独立窗口补齐 8px 热区与 4px 滑块的安全默认值，避免缺失主界面变量时退回按页面内容计算隐式列宽。设置分类栏移除原字符占位，改用统一描边和圆角端点的语义 SVG，正常宽度显示 16px 图标加文字，窄栏使用 17px 居中图标。旧版 Settings 的合并详情面板保持不变。
- 新版 stable Settings 的普通操作按钮统一为 28px 高度、最大胶囊圆角、同一半透明底色和颜色1至颜色2的渐变悬停 / 键盘焦点反馈；恢复默认、配置与完成、快捷键捕获、缓存、运行时、诊断、版本及颜色入口不再各自沿用不同高度与状态色。“嵌入元数据”移除重复的内层标题与胶囊设置行，只保留与缓存、更新一致的状态及右侧操作布局；“快捷动作”与“快捷指令”均移除旧版收起 / 展开容器和重复标题，列表在新版中常驻显示，启用等控制并入标题行。快捷指令以响应式双列命令说明表展示，避免展开前后宽度变化造成界面晃动。开关、下拉框、字体滑块、格式选择和左侧导航继续保留各自控件语义。
- 新版 stable Settings 在 700px 以下统一采用“说明在上、控件在下”的窄窗布局：普通控件与多控件组从左侧起排，状态与操作继续左右分栏；llama.cpp 和模型选择框弹性占用剩余宽度，诊断操作保持右侧紧凑按钮，不再出现控件居中、固定宽度或整行拉伸混用。
- 新版 stable Settings 的自绘滚动条改为贴附内容表面右缘，不再作为独立网格列挤占内容宽度；普通与窄窗口下的内容轨道均按完整可见区域居中，卡片左右留白保持一致。
- 修复独立 Settings 未继承主界面滚动条颜色变量，导致长页面虽可滚动且可拖动热区、滑块却始终不可见的问题；滚动与悬停显示、空闲自动隐藏及明暗主题反馈保持原有语义。
- 新版 stable Settings 的快捷指令双列间距由 44px 收紧至 24px，各分组按自身最长指令自然确定指令列宽，说明文字使用剩余空间并允许换行；英文长指令与说明不再受旧版 142px 单行省略规则截断。
- 新版 stable 主窗口左侧栏默认宽度由 160px 调整为 176px，与独立 Settings 导航栏统一；双击分隔线恢复为同一默认值，40–320px 拖动范围及窄窗口自动压缩规则保持不变。
- 修复新版独立 Settings 每次重新获得焦点都会切回全页“正在读取设置”，并导致从失焦窗口直接点击控件时首次点击被吞掉的问题；只有首次初始化和手动重试使用阻塞加载页，聚焦时保留现有界面静默同步，并合并尚未完成的重复读取。
- 新版 stable Settings 的语言、窗口材质、主题、运行时与模型选择统一改用主界面浮层样式的自绘下拉框，保留键盘导航、视口避让和点击外部关闭；llama.cpp 与模型选择框使用一致的加宽尺寸和控制区起点，长模型名称可显示更多内容且两行左侧视觉对齐。“配置外观”的主题色与副色改为复用旧版完整自绘拾色器，入口显示实时色点，同行最右侧补回统一样式的“恢复默认”操作；色板使用同款半透明圆角外框，拖动即时预览、点击外部保存、`Esc` 恢复原值。旧版 Settings 的控件与数据语义保持不变。
- 新版稳定 UI 建立统一界面字体规范：主窗口、Skim、独立 Settings、Preview 信息边栏、响应式文件菜单和窗口内弹窗统一复用正文、控件、副标题、标题及快捷键语义字号，并统一使用 Segoe UI Variable 与中文字体回退链。设置页左侧分类导航由浏览器默认字号收口为与主界面一致的 13px 正文基准；Markdown、代码、EPUB/MOBI 与字体样张等文件内容预览继续保持独立排版。
- 新版 stable Settings 的“外观”分类末尾新增五档“字体大小”滑块，按实际档位显示“小 / 标准 / 空 / 空 / 大”；拖动期间仅更新滑块，松开指针或完成键盘操作后一次性应用，避免 Settings 连续重排抖动。滑块控制区调整为 252px，使其可见轨道与上方两个颜色入口及“恢复默认”按钮的组合宽度视觉对齐。选择结果持久化并同步主窗口、独立 Settings、稳定 Preview、Portal 标题栏与窗口内浮层；旧 Cap7CE / compatibility 界面及文件内容预览不随该设置缩放。
- 新版 stable Settings 移除左侧导航栏额外的半透明白色背景与局部模糊层，让窗口材质直接贯穿侧栏；搜索框改用最大胶囊圆角且聚焦时不再显示额外描边，中文输入法组合输入期间延后筛选至候选词确认，分类胶囊和右侧内容表面保持原有层级。
- 移除主窗口与 Preview 拖动结束后按 40 DIP 阈值二次改写位置的自绘边缘吸附，避免与 Windows Snap 布局、贴靠分屏和跨屏拖动互相争夺窗口 bounds；stable 与 compatibility 原生窗口改由 Windows 完整接管贴靠布局，并在识别到系统 Snap 几何时暂停布局记忆、内容自适应改写和边缘收起。独立的边缘收起停靠判定同时由 40 DIP 收紧至 5 DIP，只有窗口真正贴近屏幕边缘时才建立收起会话；原有可选开关、5 DIP 收起边沿、line 布局及窗口越界恢复保持不变。
- 新版 stable 主窗口移除“窗口记忆”设置并将有效自由窗口布局改为默认记忆；首次启动、布局失效及“恢复默认窗口尺寸”统一按当前显示器工作区宽高的 82% 居中，并封顶于 1600×1000。旧 cap7ce / compatibility 的窗口记忆开关与布局语义保持不变。
- 新版稳定 UI Settings 将“浏览与 Skim”收口为“自定义查看”，移除已由主界面目录列表覆盖的“配置目录”及重复的页面内标题；说明文案直接作为页面副标题。格式配置改为常驻展开的响应式双列分类卡片，文件格式按钮收紧为 24px 高度；隐藏文件及与标题合并的分类全选控件使用同款滑动开关。已选格式保持主题渐变，未选格式保持透明并在悬停时显示普通圆角背景。恢复默认使用渐变悬停，窄窗口自动恢复单列。line 从浏览分类迁入“通用”的窗口设置，旧版 Settings 保持不变。
- 新版稳定 UI 的删除文件、删除目录、目录冲突、拖入与独立 Settings 清理缓存等确认改用同一套窗口内弹窗容器：卡片复用主窗口 80% 浮层表面、模糊和阴影，旧版提示图标与业务状态保持不变，确认按钮统一为 Preview 文件操作的胶囊与主题渐变反馈；删除确认不再整页替换搜索结果。新版中的浮动关键词编辑卡片同时移除额外白色蒙版，旧宿主继续保留原遮罩。
- 新版稳定 UI Preview 信息边栏移除旧版遗留的“skim 边栏”和“打开设置”入口，各信息卡背景调整为 60% 白色；文件操作按钮悬停时复用网格文件图标的主题色至副色渐变，并按两种背景色的综合亮度自动选择黑白文字，删除动作不再单独使用红字；鼠标点击边栏展开 / 收起按钮后会释放按钮焦点，使空格可以立即继续关闭 Preview，同时保留键盘导航和按钮操作能力。
- 新版稳定 UI 的侧栏与 Skim 工具栏浮层改为继承当前主界面的字体、字号和明暗主题文字色，并使用 80% 不透明度的主题表面背景；浮层操作项与文件右键菜单统一为最大圆角胶囊，并在悬停时使用主题色至副色渐变和自适应文字色。目录删除依靠既有二次确认而不再单独使用警示文字色，窗口内避让与交互链不变。
- 新版稳定 UI 主窗口左边栏的非交互背景现在可直接拖动窗口，目录、按钮、输入框与滚动区域继续保留完整交互；顶部搜索框在极窄窗口中缩小右侧保留空间并继续随窗口宽度连续缩放，以显示更多搜索内容而不在响应断点突然铺满。
- 新版稳定 UI 左边栏的 AI 增强、排序、搜索范围、目录项目、目录辅助动作及底部按钮在选中、展开或鼠标悬停时统一使用 50% 白色半透明背景；弹出浮层内的操作项仍保留主题渐变反馈。
- 新版稳定 UI 目录操作浮层将“双击重命名”简化为“重命名”；进入行内编辑后，输入框使用 50% 白色半透明胶囊背景且不显示主题色焦点描边，编辑行不再叠加额外主题色底；新版顶部搜索框同样不再显示全局输入焦点描边。
- 搜索结果沿用的关键词编辑浮层更新为新版视觉：卡片使用 80% 主题表面、12px 圆角、背景模糊及统一阴影，文件格式、文件名和大小重新按新版信息层级排布，编辑框使用无焦点描边的半透明圆角表面并隐藏内部滚动条；原有滚动能力、Enter 保存、Esc 取消、单项 / 批量更新及旧宿主蒙版语义保持不变。
- 新版稳定 UI 的 Skim 网格项增加悬停、键盘焦点及选中反馈：默认继续使用半透明表面，指向、聚焦或单击选中后切换为主题对应的 100% 不透明面板背景。
- 新版稳定 UI 的 Skim“此电脑”根页面改为系统位置、星标目录和磁盘三个分组：三个分组均可通过带悬停反馈的整行标题折叠，系统位置继续沿用原有持久化状态且收起时包含桌面，星标目录为空时自动隐藏，右键收藏的目录可立即在根页面打开并可通过自绘菜单取消星标；新版菜单与反馈同步改用“星标文件夹”语义，旧版边栏及其文案保持不变。
- 新版稳定 UI 的 Skim 面包屑在路径被压缩时仍会于右侧保留 24px 空白热区，单击空白区或分隔空隙即可进入无焦点描边的路径编辑；具体路径按钮继续执行导航且不显示选择焦点描边。
- 新版稳定 UI 的搜索结果与 Skim 右键菜单改用统一的响应式自绘单层菜单：常规高度纵向突出显示文件格式、首尾省略的真实文件名与尺寸 / 大小摘要，再分组排列六项文件动作；高度低于 360px 时自动切成“信息 + 两列动作”的三行横向布局，并在标题栏下方和窗口四周保留 5px 避让。操作项右侧补充既有快捷键说明，悬停与键盘焦点统一使用最大圆角胶囊和主题色至副色渐变。两种布局继续复用原有选择范围、快捷键及文件动作链，已停用的 Windows 原生菜单 IPC 与适配层一并移除，旧宿主与 Preview 的既有菜单保持不变。
- 新版稳定 UI 左侧栏的 AI 增强入口移除重复的滑动开关，以线框 / 实心图标和状态副文案表达停用、启用及匹配中状态，原有点击动作与可访问状态保持不变。
- 新版稳定 UI 并排 Skim 不再固定封顶于 480px，而是可调整到左侧栏之外主内容区的一半；搜索结果与 Skim 最多等宽显示，窄窗口已有的结果替换规则保持不变。
- 新版稳定 UI 的 AI 增强、搜索范围、目录、Skim、设置与主窗口 / Settings 搜索框改用可随主题变色的独立 SVG：开关、菜单、目录选择和 Skim 展开状态使用对应实心或打开形态，设置按钮仅在按下期间显示实心反馈；主侧栏与 Skim 工具栏的排序恢复旧版升降序图标，标题栏固定按钮同步放大到与系统标题栏图标接近的尺寸。
- 新版稳定 UI Settings 保持 860×680 默认窗口、620×480 最小窗口和八类真实设置内容，左侧分类栏为 176px，搜索框与分类行为 34px / 36px；右侧普通设置不再逐条使用长胶囊，而是按标题分组共用一块 22px 圆角背景，内部以连续设置行和轻分隔线整理复杂内容。自定义查看维持独立的响应式格式卡片，窄窗在 700px 切换为 52px 分类轨道。
- 新版稳定 UI Preview 的文件信息栏改用 40px 折叠轨道与 280–420px 可调卡片式详情布局：文件概况、位置、手动关键词、匹配依据、嵌入信息与文件操作沿用真实数据和既有动作，以 22px 信息卡、胶囊标签及单列操作按钮统一视觉层级；旧 Preview 不变。
- 新版稳定 UI 的正式搜索与 Skim 虚拟网格改用空壳尺寸规范：搜索缩略图维持约 150px 并在极窄窗口至少保留两列，Skim 使用独立约 120px 目标尺寸，两者统一为 5px 间距、四边等效 10px 页内留白和 8px 卡片圆角；纵向与低高度横向滚动条分别位于网格右侧和底部的独立 8px 区域，其外保留 2px 留白，低矮窗口隐藏侧栏后在内容表面左侧补齐 5px 窗口边距，最小整窗高度为 170px，展开的 Skim 独占可用宽度；旧宿主的网格密度、最小内容高度与布局语义保持不变。
- 全局原生滚动条与共享自绘滚动条统一使用主题感知的半透明中性色及悬停层级，stable 主窗口目录列表与 Preview 信息边栏接入同一套自动隐藏自绘滚动条，不再保留与网格不同的原生滚动条外观；自绘滑块使用无描边、无阴影的 4px 主体，悬停时扩展至 6px。
- 新版稳定 UI 主窗口左侧栏改用正式 Cap7CE 矢量标志，并按新版空壳统一全部目录、普通目录与底部 Skim / Settings 按钮的纵向间距；真实目录计数、管理动作、紧凑侧栏和明暗主题能力保持不变。
- 新版稳定 UI 完成主题与可访问性收口：主窗口不再由系统媒体查询覆盖用户固定选择的明亮 / 黑暗模式，两种自定义颜色分别进入选择强调与键盘焦点；主窗口、Settings 和 Preview 补齐可见焦点、键盘分隔线、减少动态效果与中英文长内容溢出保护。D0 在不删除旧外壳、不迁移既有旧模式偏好的前提下，将该界面提升为正式默认宿主。
- 新版稳定 UI 正式宿主使用 `frame: true`、40 DIP Window Controls Overlay 和 Acrylic，系统不支持时回退明暗主题安全纯色；首次自由窗口按工作区宽高的 82%、最大 1600×1000 居中，布局记忆使用独立 `window-layout-stable-ui.json` 中的单一自由 bounds。主窗口、独立 Settings 与 Preview 可以同时存在，普通关闭 Preview 不重复显示或改变主窗口几何，但会交还键盘焦点，确保同一缩略图可立即再次用空格预览；编辑关键词、删除和切换 Skim 位置仍会明确回到主界面。micro / mini 快捷项在新版隐藏且不注册，normal 只保留为用户主动触发的一次性默认尺寸预设；原 Capsule 快捷键与 line 点击只显示主窗口并聚焦搜索，旧界面和旧 Capsule 回退链保持不变。
- 新版稳定 UI Preview 增加快速确认交互：普通滚轮与左右键继续切换文件，边栏、菜单和音视频控件隔离切换手势；图片支持以指针为中心的 `Ctrl + 滚轮` 有界缩放，仅在内容溢出时允许受限拖动，切换文件、窗口或边栏尺寸变化后自动复位或重新夹紧。图像区域使用与新版主表面一致的明暗半透明背景；自由窗口继续按内容比例调整，并按信息栏实际宽度、右侧与底部各 5px 计算内容外框，展开信息栏时在工作区允许范围内扩展窗口以保持图片区域；最大化与 Windows Snap 状态不改写窗口 bounds，各格式仍复用原 Provider 与缓存链。
- 新版稳定 UI 开发入口接入正式 Preview 壳层：继续复用搜索结果与 Skim 的单实例 Preview、Provider、关闭恢复和文件动作链，在左侧加入可收缩、280–420px 可调并记忆状态的信息边栏；文件属性、位置、手动关键词、用户描述、当前匹配依据及嵌入信息统一进入边栏，旧版预览与打包入口保持不变。快速切换、图片缩放和拖动留在下一轮接入。
- 新版稳定 UI 独立 Settings 接入正式设置内容：按通用、外观、浏览与 Skim、搜索与 AI、缓存与索引、快捷键、诊断、关于八类组合现有偏好、目录、缓存、运行时 / 模型、快捷键捕获、诊断导出与更新任务；分类内搜索支持中英文正式文案，主窗口与 Settings 共享同一份偏好和目录状态。窗口材质只显示 Acrylic 目标只读状态，暂不提前改变宿主材质或提供虚假选择；未逐项对应的新旧文案允许在后续轮次继续补齐。
- 新版稳定 UI 开发入口建立独立 Settings 窗口宿主：主界面按钮、全局快捷动作、托盘、通知和 Preview 统一复用单实例打开动作，重复请求只恢复并聚焦；关闭只隐藏 Settings 自身，主窗口与 Preview 状态不变。Settings 使用独立版本化 bounds 记录并在显示器缺失或工作区变化后恢复到可见区域；旧 Cap7CE 外壳继续回落原同窗设置页，正式设置内容留待后续迁移。
- 新版稳定 UI 开发入口接入正式并排 Skim：右侧面板复用现有目录读取、虚拟网格、缩略图、Preview、右键菜单、文件拖出 / 复制、目录添加与边栏收藏链，增加返回、可点击面包屑、双击地址编辑、独立排序和查看范围；隐藏面板保留路径、选择与滚动状态并停止响应文件快捷键，窄窗和低矮窗口只改变可见区域及网格方向，不建立第二套 Skim 或搜索状态。
- 新版稳定 UI 开发入口接入正式左侧栏与目录能力：AI 增强、排序和搜索范围显示真实状态并复用现有搜索动作，全部目录与单目录复用既有加载、选择、添加、拖入确认、冲突替换、重命名、删除及结果刷新事务；侧栏支持 40px 紧凑图标栏、长名称省略、独立滚动和底部动作锚定。Skim 本轮仍切换占位区，Settings 暂时回落到现有设置页，不提前建立真实 Skim 或独立设置窗口。
- 新版稳定 UI 开发入口接入正式搜索与结果主链：顶部输入复用现有查询、快捷指令、路径直达、AI 按次入口和清空查询刷新，中央区域直接复用现有虚拟网格、证据分组、选择、滚动记忆、右键菜单、Preview、拖出、复制、关键词和删除动作；低高度只改变网格滚动方向，不建立第二套搜索状态或按 micro / mini / normal 切换新版组件树。
- 新版稳定 UI 开发入口建立不含真实数据的响应式空壳：侧栏、搜索结果区与 Skim 占位区随窗口连续重排，侧栏和 Skim 宽度可拖动并双击恢复默认值；窄窗口由 Skim 替换中央内容，极窄窗口只保留 Skim，低矮窗口改用横向占位网格。该空壳不接入搜索、目录或 Skim 业务，也不恢复基于拖放尺寸的 micro / mini / normal 自动状态切换。
- 新版稳定 UI 增加仅开发环境可进入的兼容宿主根节点，建立独立设计变量、内框表面和滚动条基础，并让现有兼容主窗口、兼容 Preview 与新版根节点复用同一个可访问置顶按钮；生产 Renderer 不包含新版开发根节点，旧界面和默认窗口模式保持不变。新版开发窗口采用单一自由缩放入口，使用独立布局记录和会话级置顶状态，并只在该入口旁路旧 resize settle 的 micro / mini / normal 自动形态转换；不冻结正式尺寸或快捷键语义。
- 冻结新版稳定 UI 正式接入前的 0.9.9 基线：记录现有搜索、目录、Skim、Settings、Preview、文件动作、快捷键与窗口恢复链到新版入口的迁移映射，并由架构检查守护源码所有权、热点文件体量和本机原型隔离；本轮不改变用户可见界面或窗口行为。
- 兼容模式标题栏的固定窗口按钮改为与系统最小化、最大化和关闭按钮共用同一明暗主题符号色；悬停继续仅显示原生风格背景，置顶状态继续通过图标形态区分，不再额外改变图标颜色。
- 图片预览改为按源文件负担决定是否生成缓存：普通 JPG / JPEG / PNG / WEBP 在最长边不超过 4096、总像素不超过 1600 万且文件体积不超过 20 MiB 时直接读取原文件，任一项超限才生成 2560px PNG，避免把轻量压缩图片无意义地转成更大的无损预览缓存；GIF、动态 WEBP、APNG 及复杂格式保持既有预览语义。
- 关键词编辑卡片增加低透明度明暗主题纯色蒙版，在保留结果网格上下文的同时弱化高对比缩略图；不使用整页模糊或额外 GPU 滤镜。

### 修复

- 修复新版稳定 UI 主窗口缩窄至 760px 以下并隐藏文件数量后，固定窗口按钮因失去原有自动占位而紧跟搜索框左移的问题；窄窗口下固定按钮现在继续贴合 Windows 原生窗口控制区，常规宽度布局不变。
- 修复正式 stable Preview 在移除旧窗口控制栏后没有装配新版 WCO 标题栏，导致固定窗口按钮、标题栏拖动和 Windows 原生最小化按钮同时缺失的问题；stable Preview 现在通过独立 Portal 标题栏复用固定动作，只在 stable 宿主启用原生最小化并显示独立任务栏入口，同时清除旧透明根节点遗留的重复边框与圆角。旧 cap7ce / compatibility Preview 行为保持不变。
- 修复正式 stable 主窗口首次显示时没有触发旧版空查询结果链、导致“全部目录”中央网格为空，同时 Skim 右栏被默认展开并提前读取“此电脑”的问题；目录与偏好初始化后现在只执行一次全部目录搜索，Skim 只在用户点击按钮后展开和读取，stable 空结果区域仅保留结果文案且不再作为进入 Skim 的点击热区。
- 修复新版稳定 UI 用空格关闭 Preview 后，键盘焦点仍停留在已隐藏的预览窗口、导致同一缩略图必须重新点击或切换选择才能再次按空格预览的问题；关闭 Preview 现在保留三窗口并存与主窗口几何，同时把键盘焦点交还主窗口。
- 修复新版稳定 UI 结果网格向下滚动后，主窗口标题栏可能失去 Windows 原生拖动命中的旧问题回归；稳定与兼容 WCO 标题栏现在统一通过共享 Portal 宿主脱离虚拟网格及其他会滚动、裁剪或动画的内容树，并由共同自动守门禁止后续界面迁移再次绕过该隔离边界。
- 修复新版稳定 UI 开发入口点击 Windows 原生关闭按钮后，旧 standby 请求被新版形态隔离直接忽略、导致窗口看似无响应的问题；新版现在保留任务中拒绝隐藏的安全检查，空闲时继续进入现有 standby / line 隐藏链，旧界面关闭与托盘语义不变。
- 修复兼容模式在 125% / 150% 等非整数 DPI 缩放下，右侧贴边窗口反复收起与展开时可能持续写回系统坐标取整误差、逐轮移出屏幕并最终丢失自动收起会话的问题；停靠会话现在固定首次识别到的边缘间距，并在每次记录恢复坐标时重新对齐当前工作区，既有四向贴边、5px 间距和交互语义不变。
- 修复开发版在三种窗口宿主之间受控重启时，开发任务管理器会把 Electron 的正常退出误判为整组任务结束、继而关闭 Renderer 页面服务并让新进程显示空窗口的问题；任一开发子进程异常退出仍会终止整组任务。打包版更新与模式切换链路不受影响。
- 修复兼容模式预览窗口最大化后点击系统关闭按钮，Windows 退出最大化时可能把已清空的预览窗口重新显示为空白外壳的问题；关闭链路现在先结束最大化，最后确保窗口隐藏。
- 清空搜索框内的搜索词后立即执行空搜索并返回默认全部文件网格，移除原有 500ms 等待。
- 修复较慢设备、高 DPI 缩放或双显示器环境中，micro 贴边展开 / 收起后的延迟原生移动事件被再次识别为用户吸边、导致窗口无法保持收起或网格轻微抖动的问题；整个停靠会话不再参与普通移动 / 缩放结算，用户实际拖动或拉伸时会先结束旧会话，显示器内部接缝也不再作为可收起的物理屏幕边缘。
- 修复企业加密或异常视觉文件大量进入当前结果网格时，交互式缩略图对未变化来源反复调用原生解码、并在最小化 / 恢复后形成失败风暴的问题；失败现在按文件源版本在本次进程内抑制，文件变化或手动清理缓存后允许重试，窗口最小化或隐藏时丢弃不再可见的排队请求，并限制连续失败诊断写入频率。
- 修复正式搜索结果长按空格期间因结果数据或父级回调刷新而重装键盘监听时误取消计时器、导致选中蒙版停在主题色但关键词编辑卡片不弹出的问题；真实失焦、选择变化、视图切换及其他浮层仍会取消长按。

## 0.9.8

### 优化

- AI 搜索自动启动的 llama-server 在任务完成、暂停、取消或失败后保温 10 分钟，期间新任务继续复用模型，空闲超时后自动停止以释放内存与显存；用户从 Settings 或快捷指令手动启动的服务仍保持运行直至显式停止或退出软件。
- 顶部“AI增强”标签的悬停提示与其他可隐藏标签统一为“右键单击隐藏标签”；原处理进度改由“AI 匹配中 / AI 已暂停”状态卡持续显示，并继续复用同一任务统计来源。
- 优化预览嵌入信息浮层：移除会被预览容器边界截断的外部阴影，并将标题改为适配明暗模式的常规界面文字色；保留半透明背景与模糊效果。
- 精简 Settings“自定义查看”的中英文格式统计说明，在保留已选数量与“自定义”应用提示的同时避免英文长文案挤入右侧操作按钮。
- Settings 中的 llama.cpp 版本与模型选择框统一为 230px × 20px，运行按钮移至版本框前方，使两行选择框及刷新按钮上下对齐。
- Settings“配置目录”同时显示动态目录数与全部受支持文件总数；normal 默认全目录且无搜索条件时改为显示同一统计来源的“文件数量”，输入关键词或切换目录 / 格式筛选后仍显示当前“搜索结果”。
- 校正 Settings“快捷指令”的帮助清单与实际执行链：补齐目录路径添加、名称 / 修改时间排序、查看范围与 AI 标签显示、贴边收起、系统通知、自动缓存优化和 AI 深度匹配开关；`cache:thumb` 复用确认机制，仅清理正式搜索的普通与 Shell 缩略图缓存，已移除失效的旧索引及未实现缓存指令。

### 修复

- 修复右侧窗口控制栏按钮经鼠标操作或窗口重新聚焦后偶发残留键盘焦点框；鼠标点击不再保留焦点，`Tab` 键盘导航仍显示原有焦点提示。
- 打包版统一关闭主窗口、预览、line 与启动提示窗口的 Chromium DevTools 能力，`Ctrl+Shift+I` 不再打开调试窗口；开发环境继续保留调试能力。
- 修复独立预览窗口右键菜单缺少搜索结果页已有的快捷键标注和实际按键入口；通用查看 / 操作项改由共享构造器与组合键解析统一，预览菜单打开时 `Esc` 优先关闭菜单，正式搜索预览支持长按空格返回关键词编辑，skim 权限保持不变。
- 补齐 skim 右键菜单“添加目录 / 加入或移出边栏”的快捷键标注与实际按键入口，分别使用 `Ctrl+Shift+D / Ctrl+Shift+B` 并沿用当前单选或多选范围；用户收藏可直接移出，系统固定位置仍保持不可移除。

## 0.9.7

### 调整

- Cap7CE 自有源代码改以 PolyForm Noncommercial 1.0.0 提供，允许协议定义范围内的非商业使用、研究、修改与分发；任何商业用途均需事先取得维护者的单独书面授权。第三方组件继续适用其各自许可证，现有功能与用户数据格式不变。

## 0.9.6

### 新增

- 新增本地运行诊断：默认以低开销轮转日志记录启动、搜索耗时与资源快照、缩略图失败、Renderer 无响应及进程异常，并启用不上传的本机 Crashpad 崩溃报告；Settings“运行信息”可复制日志 / 崩溃目录完整路径、在本次运行中临时开启详细记录，并导出经过路径脱敏和容量限制的诊断 ZIP。
- AI 增强搜索 Beta 改为按需逐图级联：结果页可点击“AI增强”或使用查询前缀 `7ce/`，基础结果立即展示，随后仅对确定性条件缩小后的剩余候选逐图、逐视觉词判断。每项使用温度 0、1024 最低视觉 token 与简短单条件 `0/1/2` 请求，首个不满足条件即停止该图；各独立条件由程序求交集，不再追加容易误拒绝的复合模型请求。明确全部满足才追加“AI 深度匹配”；任务默认持续至完成，结果分隔卡实时显示匹配中、用户暂停或完成状态并可整卡暂停 / 继续，顶部“AI增强”只负责总开关，关闭时彻底结束本次任务但保留已显示结果。
- 建立可独立启停的 AI 查询确认词缓存边界；Beta 准确性测试阶段暂不写入或读取查询标签，AI 判断只影响当前会话结果，避免误判污染后续普通搜索。表结构、源 / 模型 / 提示词版本失效与每文件 48 项上限保留供后续验证通过后启用，不写入或覆盖人工关键词。
- 建立高价值嵌入元数据的后台增量索引：新扫描文件按源版本与提取器版本跳过未变化项，窗口聚焦时自动增量降低处理频率、失焦后恢复正常速度；旧索引可在 Settings 中显式补齐并随时取消，用户主动补齐不受前台限速；提取工作位于独立 Worker，不绑定搜索请求或冷启动全量扫描。
- Settings 中的嵌入元数据行改为按任务状态显示“检查补齐 / 停止补齐 / 继续补齐”；空闲或正常完成时隐藏冗余说明，仅在运行、停止或失败时展示必要进度，手动检查没有待处理文件时短暂反馈“元数据已完整”。
- 搜索结果保留原有证据来源与可信度排序，展示标题卡精简为“快速匹配、可能相似、AI 深度匹配”三类；标题使用颜色1，副标题跟随当前明暗模式的右侧窗口按钮颜色。
- 正式搜索可按既有空格分词和跨来源 AND 规则匹配文件内的高价值标题、描述、正向画面内容、软件家族、拍摄设备、媒体及字体身份；该来源独立参与证据排序与分类，只向界面传递最长 180 字符的命中片段，不发送完整 Prompt。
- 正式搜索可将透明 / 无背景、白底、黑底、暗图、灰色 / 灰色调以及经真实样本确认的粗颜色词映射到低级视觉属性；透明背景作为确定性自然条件，其余仅进入“可能相似”弱证据分类，缺少或已失效的属性不会排除文件名、路径、人工关键词等基础命中。灰色只表示画面整体接近中性灰阶，不判断局部物体颜色，单字“灰”仍作为普通文本。
- 正式视觉文件可按源图实际尺寸匹配横图、竖图、方图及 `16:9` 等精确宽高比；尺寸尚未完成、读取失败或格式不在轻量读取范围时不作为反证，文件名、路径、人工关键词等基础证据仍可正常返回。
- 正式搜索可按已确认的容器动画事实匹配“动图”：后台以受限顺序读取区分多帧 GIF、动画 WebP 与 APNG，静态同格式文件不会因扩展名被误判；AVIF 与动态 SVG 暂不纳入。
- 自然条件词表补充分类型文件与日常日期表达：图片 / 图像只表示普通栅格和相机图像，设计源文件、文档 / 文本及 Office 子类保持可解释边界；支持刚刚、前不久、前段时间、很久以前、`2026年6月`、`2025年4月29日`、`2025/4`、`2025/4/29`、`去年5月`等明确范围，同时保留同词普通文本命中。
- 后台嵌入元数据、源尺寸、视觉属性或动画事实写入成功后，合并通知当前结果查询自动刷新；新索引无需重启软件即可参与搜索，同时以主进程与 Renderer 双重防抖避免批次写入造成重复查询。
- 空格预览按当前文件路径即时读取已有嵌入元数据：通用文件信息与音频预览追加结构化字段，文本文档在标题区显示紧凑摘要，图片和视频通过底部可折叠浮层查看完整索引内容；没有元数据时不新增空白界面。

### 优化

- Settings 原“详细信息”统一改为“运行信息”，按 Cap7CE 与 llama.cpp 两组展示；应用日志、崩溃报告显示可选择复制的完整路径，原“运行日志”明确改名为“llama.cpp 日志”。
- 自绘滚动条的横纵最小滑块尺寸统一由 40px 增至 56px，提升长列表中的可见性与拖动命中。
- 主窗口与预览窗口收起到屏幕边缘后临时使用较低的浮动层级，确保露出的真实窗口边沿不被普通窗口遮挡；触边展开后立即恢复原有置顶状态并提升至普通窗口前方，不抢占当前输入焦点。
- 边缘收起时保留的真实窗口边沿由 8 DIP 缩减至 5 DIP，减少停靠后的视觉占用，同时保持屏幕最外侧触发范围不变。
- 新用户首次启动时，正式搜索的默认排序由名称降序改为时间降序；已有用户保存的排序偏好与 skim 独立排序不受影响。
- Settings 的全局 AI 开关统一命名为“AI 深度匹配”，补齐按需匹配过程中新增长状态与错误反馈的中英文文案；当前评分链路不生成标签，未来恢复模型标签输出时仍须跟随已解析的界面语言。
- 建立低级视觉属性的纯分析与紧凑存储边界：从受限 RGBA 代表图计算 Alpha、粗颜色族、明暗、饱和度和外围背景数值，以 0–10000 定点整数按文件级联保存并由源版本 / 算法版本失效；不在文件记录中固化“透明背景”等自然语言，后续同义表达调整无需重算图片。
- 低级视觉属性接入正式 300px 搜索代表图的成功链路：缓存新生成或既有缓存被自动优化发现后，以独立 Worker 低速增量分析并批量写库；缩略图先返回、分析通知不被等待，未变化项按源版本 / 算法版本跳过，失败与目录删除候选相互隔离。
- 栅格与 SVG 源尺寸改由独立低优先级 Worker 在扫描后增量补齐，直接复用 `images` 既有宽高字段，不依赖自动缓存开关，也不在查询时读取源文件；前台活动时降低处理频率，源文件大小或修改时间变化后只清空并重算对应文件。
- 文件类别同义词进一步贴近日常表达，并主动排除高污染短词：新增原文件、声音、幻灯片、表格、ps文件、横版 / 竖版、深色和粉等受限映射；照片、正方形及图、表、灰、暗、ps等词不再或不被解释为宽泛固定条件，仍可按文件真实文本搜索。
- 完成 Renderer 页面、设置区块、领域样式、交互控制器与主进程 IPC 注册的分域整理，并以自动架构检查守护依赖方向、热点文件体量和新增 IPC 边界，为后续功能扩展保留稳定维护入口；纯迁移阶段保持既有交互与视觉行为。
- Settings 的 AI 区域从全目录完成度管理收敛为默认开启的全局【开启 / 关闭】能力开关；移除支持文件 / 已识别 / 未识别三项完成度统计、继续识别、全部更新、单目录识别及其旧 IPC。搜索页同步移除识别状态筛选、专用未识别列表和旧快捷指令，所有已登记文件统一进入普通结果网格；normal 右上角继续显示支持文件总数。
- 自动缓存优化在搜索、skim、Settings 与预览窗口聚焦时保持单任务并显著降低领取频率，失焦、最小化、隐藏或进入 line 后恢复正常后台速度；当前可见缩略图仍按原优先级即时加载。重启后若开关仍启用，会重新发现缺失或源文件已变化的缓存并恢复队列，无需切换目录触发。
- 关闭待机 line 后销毁其隐藏 Renderer，再次开启时按需重建，降低不使用 line 时的后台资源占用。
- 统一 Settings 目录数量与 normal 页“全部文件”的受支持格式口径；更新 skim 边栏系统文档图标。

### 修复

- 修复 normal、mini 与 micro 共用原始像素滚动偏移，切换窗口形态后浏览位置逐步向结果前方漂移的问题；同布局继续精确恢复原偏移，跨布局改为围绕同一真实文件恢复，原本位于末尾时保持末尾，文件失效时才按相对进度回退。
- 修复已添加目录外部新增文件后，结果页按 F5 仅更新当前内存扫描结果、重启后新增内容再次消失的问题；显式刷新现在复用目录扫描写入正式索引并更新文件计数，普通搜索与冷启动仍不额外触发全目录写入。
- 修复自动缓存优化开启且大目录缩略图已经完整时，正式搜索和清空搜索会逐项重复广播全部既有缓存、继而触发大量视觉属性检查并造成数秒无响应和内存峰值的问题；同一缓存身份现在每次运行只通知一次，首次发现大量旧缓存时分批让出前台，关闭或重新开启开关会使旧发现任务失效。
- 修复 ComfyUI 等嵌入信息已经可以参与搜索，但从搜索结果打开图片或视频预览时因缺少源文件修改时间、无法通过版本校验而不显示信息浮层的问题。
- 修复源文件内容变化后按 F5 仍复用旧缩略图、文件大小和修改时间的问题；缩略图请求现在携带由文件大小与修改时间生成的源版本，关键词命中结果也会先合并最新文件元数据，使修改时间排序同步刷新。
- 修复关键词编辑、删除确认等临时交互在窗口收起后残留，随后通过部分快捷键恢复时可能出现透明窗口、弹层叠加或 skim 边栏失效的问题；进入待机前统一取消未提交交互，正在执行的破坏性或异步操作继续阻止收起。
- 修复冷启动后通过托盘打开 normal 偶发缺少缩略图网格的问题；托盘图标改为单击并复用 normal 快捷键的统一恢复链路，同时保留已有 skim 会话。
- 修复 Settings“详细信息”展开后标题位置和整行点击区域变化，以及删除目录确认结束后已展开目录列表自动收起的问题。

## 0.9.3

### 新增

- 搜索结果页可通过本机 Windows Shell 尝试为受支持的视频格式生成缩略图；成功时叠加清晰但不遮挡内容的播放标记，系统缺少对应解码能力或生成失败时继续显示格式图标。
- 统一搜索输入支持粘贴完整 Windows 文件路径后进入其所在 skim 目录；继续兼容目录路径、正反斜杠、成对双引号与 UNC 路径，不自动改变文件选择状态。

### 优化

- skim 入口悬停文案统一为“skim 边栏 / 收起 skim 边栏”，替换边栏底部操作图标，并为 mini 单独采用更紧凑的边栏尺寸；normal 保持原有视觉比例。
- Office 预览在同一进程内复用未变化文件的 Excel 与 PowerPoint 转换结果，重复打开时不再重新执行转换；源文件大小或修改时间变化后自动失效。
- 关键词编辑提示文字统一使用搜索框操作提示色；自定义查看的格式统计与启用说明进一步明确，非视觉文件在未识别筛选中补全状态文字。

## 0.9.2

### 新增

- skim 快速访问边栏新增 Windows 桌面入口，并可收起或恢复下载、文档、图片、音乐和视频等系统目录；“此电脑”和桌面始终保留，用户加入的星标目录不受影响。
- 统一搜索输入支持粘贴完整 Windows 绝对目录路径后直接进入对应 skim 位置；兼容反斜杠、正斜杠、成对双引号与 UNC 路径，主进程确认目录真实可读后才切页。
- skim 与搜索结果页可通过本机已安装的 Windows 图像扩展尝试显示 HEIC、HEIF 与常见相机 RAW 缩略图和空格预览；系统不支持对应格式、机型或解码失败时安全回退格式图标与文件信息，不扩大自动优化或 AI 识别范围。
- 自动更新成功并重启新版后显示系统通知，明确提示 Cap7CE 已更新至当前版本。

### 优化

- 更新下载支持从设置页主动取消并清理本轮临时文件，进度文案精简为百分比和已下载 / 总大小；失败原因区分网络、安全软件、文件校验与启动更新助手等环节，避免无效反复重试。
- 自动缓存优化对同一缓存身份在单次运行中的生成失败只尝试一次，避免搜索、目录切换或查看全部目录时反复处理无法生成缩略图的未变化文件；文件变化、进程重启或重新开启自动优化后仍可重试。
- Settings 将可选处理入口明确命名为“AI 识别”；预览中的说明类次级文字统一使用可读的主题副色。
- 顶部“全部文件”与各目录文件数量之和统一统计全部受支持文件；“已识别”表示带有 AI 或用户手工关键词的文件，“未识别”表示没有任何关键词的文件，并与搜索结果页保持相同口径。

### 修复

- 修复中断或失败的更新下载可能遗留不完整文件、后续下载无法正常写入或打开的问题。
- 修复窗口从后台恢复时可能丢失 skim 页面，以及 skim 已最小化后再次使用 skim 快捷键可能只显示透明空窗口的问题；托盘双击同样保留当前 skim 状态。
- 修复独立预览窗口打开快速访问边栏后，边栏目录偶发无法进入 skim 的状态链路；退出并再次进入 skim 后仍可持续切换目录。
- 修复从 skim 根位置使用鼠标侧键返回搜索结果后，再次前进只恢复页面外壳而不恢复目录内容的问题。
- 修复快速访问边栏内容是否溢出会改变主窗口右侧拖动行为的问题，同时保留点击其他区域自动收起边栏。

## 0.9.1

### 新增

- skim 入口改为展开或收起右侧快速访问浮窗，可从“此电脑”及 Windows 下载、文档、图片、音乐、视频目录直接进入 skim；已在 skim 中时仍可打开浮窗切换位置，并通过底部退出按钮返回原视图。
- skim 文件与文件夹右键操作支持“加入边栏”；文件会取其父目录，多选自动去重，已全部加入时禁用入口。用户加入的目录使用星标文件夹图标，可在浮窗右键取消快速访问并持久化保存。

### 优化

- 添加目录后复用文件数量扫描同步登记正式视觉文件与通用文件目录，但不自动启动 AI；顶部识别状态随即按视觉文件关键词是否为空更新“全部文件 / 已识别 / 未识别”，用户手工关键词与 AI 关键词使用同一判定。
- 快速访问浮窗复用 skim 网格、主题渐变、自绘滚动条和右键菜单方向动效；窗口缩放、切页或其他操作会自动收起，避免在 micro 中残留。

### 修复

- line 点击仅复用既有 capsule 激活链路，不绑定具体快捷键或新增窗口切换分支；capsule 支持 `Esc` 与点击外部区域收回，并恢复与其他窗口一致的底部间距。
- 收起到 line 时保留当前 skim 页面，随后通过 micro、mini 或 normal 快捷键恢复时回到原 skim，而不是强制切换到搜索结果。

## 0.9.0

### 新增

- 正式搜索结果支持长按空格约 350 ms 打开关键词编辑卡片，短按空格继续打开预览；长按计时不依赖系统键盘重复速度，并会在失焦、选择变化、视图切换或其他浮层出现时取消。
- micro、mini、normal、skim 与 Settings 支持按 `F5` 主动刷新当前页面状态；仅在窗口处于前台且没有文本编辑或阻塞交互时执行，不引入后台文件监听。

### 优化

- 将原全内容区关键词编辑界面收敛为复用右键菜单视觉、动效和边缘避让的轻量浮层卡片；单选与多选分别显示文件或批量组成摘要，Enter 保存、Esc 取消，中文输入法组合输入不会误提交，失败后保留原输入以便重试。
- 关键词卡片继续复用既有单选与批量差量写入：保留视觉文件 caption、各文件私有关键词、视觉与非视觉混合更新、事务失败反馈，以及保存后的真实搜索和滚动状态。
- skim 中长按空格与点按保持相同行为，不再因键盘重复事件造成预览窗口闪烁；正式搜索长按进入关键词编辑后会隔离并吞掉本轮空格释放，避免空格写入文本框。
- 通过全局快捷键唤起 Capsule、micro、mini、normal、skim 或 Settings 后，搜索输入框默认获得焦点，便于直接搜索；关键词编辑期间继续保持专用输入焦点和交互保护。
- 自动缓存优化优先让当前可见缩略图完成，停止浏览后在后台低速继续推进；关闭开关会停止领取新任务，重新开启后恢复队列，目录删除、缓存清理及 AI 暂停原因保持互不干扰。
- Settings 配置目录右侧数字改为当前目录内全部受支持文件数量，并补充“文件数量”悬停说明；自定义查看按钮统一使用简洁的点击式悬停文案。
- 主窗口右侧控制按钮及 skim 图标背景统一使用 210 ms 渐显渐隐 hover 过渡；关键词编辑输入框沿用预览信息面板的明暗模式背景色。

### 修复

- 修复打包版直接以 Windows detached 子进程启动 PowerShell 时脚本正文不执行，以及普通子进程又会随 Electron 退出而终止的问题；主进程改为在本轮下载目录之外生成纯 ASCII 的 VBScript 启动器，将可能包含中文的路径与参数封装为 PowerShell 编码命令，再经 Windows Shell 隐藏运行更新助手，避免系统代码页破坏参数边界、助手清理临时目录时删除仍在执行的启动器，以及用户误关命令行中断验证与回滚；待助手完成 ZIP 解压与结构校验并发出就绪信号后主程序才退出，启动器由重启后的新版延迟清理。助手预检失败会写出独立失败信号，主程序保持当前版本继续运行，并将最后一次失败原因与助手输出保存到固定临时日志，不再随本轮下载目录删除；更新下载连续 60 秒没有收到数据时主动中止并提示重试，避免网络连接停滞后无限等待。
- 将 line 从主窗口的极窄待机形态拆分为不可聚焦、鼠标穿透的独立展示窗口；主窗口按 normal 尺寸隐藏初始化，进入 line 时只隐藏主窗口，快捷键与托盘从正常窗口状态恢复，并在目标形态完成布局后再显示主窗口，避免隐藏 line 后重启无法唤起界面、位置漂移或唤起时短暂闪现旧 normal 外形。
- 固定打包版 Windows 应用标识，使覆盖更新到同一程序目录时继续沿用相同任务栏通知区域身份，减少历史版本形成重复 Cap7CE 开关项。
- 删除已添加目录后同步取消该目录尚未领取的自动缓存优化任务，并等待正在处理的单文件自然结束，避免已删除目录继续积累缓存。

## 0.8.3

### 新增

- Settings 的用户主动更新流程支持在软件内下载受限命名的 Windows x64 ZIP 并显示进度；打包版下载完成后启动可见 PowerShell 更新助手，备份并替换程序目录、保留 `models` 与 `llama.cpp`，随后重启新版，启动失败时自动恢复旧版。开发版不执行自动替换，检查与下载仍不会在后台静默触发。
- 快捷动作新增仅在搜索结果界面生效的“目录切换”，默认使用 `Alt+Q` 按“所有已添加目录 → 各已添加目录”顺序循环并保留当前关键词、查看范围、识别状态与排序；键盘及手动目录切换统一在搜索框内提示目标目录，长按产生的重复按键不连续触发搜索。
- 搜索结果与 skim 支持选中单个或多个文件、文件夹后按 `Ctrl+C` 调用 Windows 系统文件剪贴板，随后可直接粘贴到资源管理器等目标位置；磁盘盘符不参与复制，文本输入区域保留普通文字复制，成功或失败结果统一显示在搜索框内。
- 正式搜索与 skim 的共用预览窗口新增 Markdown 排版预览；`.md` 在原有 1 MiB、UTF-8/UTF-16 与截断边界内渲染标题、列表、引用、代码、表格、任务列表等常用语法，原始 HTML、图片资源与链接跳转保持禁用，其他文本和源码格式继续显示原文。

### 优化

- 搜索结果与 skim 的目录、查看范围和排序标签统一在搜索框内显示切换结果；查看范围明确提示默认、全部或自定义格式，排序提示名称或时间及当前升降序，系统反馈不写入或清除真实搜索关键词。
- 搜索结果与 skim 统一视觉文件加载失败、非视觉文件和未知文件的格式图标与两行文件名卡片；通用文件和音频预览同步整理内页高度、边距与暗色文字可读性。
- 统一 micro、mini、normal 的右键菜单基础尺寸并收紧次级操作宽度；文件信息区域悬停可查看完整名称、大小与尺寸，搜索结果及独立预览继续共享相同信息来源。
- Settings 的自定义查看分类按图像、视频、音频、文本、文档、项目、三维、压缩包、数据、字体、模型重新整理并按扩展名排序；可展开配置按钮在展开期间保持主题色状态，运行时详细信息入口维持原交互。
- Settings 快捷指令改为按内容连续排列，搜索框内临时反馈、快捷指令确认和常驻操作提示拆分配色；skim 缓存清理结果回到所属设置行显示。
- 小窗口向大窗口及反向切换复用成对的尺寸过渡；系统通知改用独立优化的 64 px PNG 图标，提升 Windows 通知中心显示清晰度。

### 修复

- 修复后台自动缓存优化只处理部分队列、窗口无操作时停止领取任务以及 Settings 进度只在重新进入页面后刷新的问题；显式关闭仍可暂停，开启后队列会持续推进并实时更新状态。
- 修复 Settings 与结果页经鼠标侧键往返后出现未激活设置页、设置按钮需要重复点击才能返回的历史导航状态问题。
- 修复 skim 在不同目录层级间共用上一层滚动偏移、导致进入目录或侧键返回后新层级从中后段开始显示的问题；路径成功切换时同步重置真实滚动容器与虚拟列表偏移，当前目录刷新不受影响。
- 限制同一用户会话只运行一个 Cap7CE 实例；再次启动时唤醒现有窗口并发送系统通知，不再建立第二套托盘、索引或后台任务。
- 恢复搜索结果与 skim 在 micro、mini、normal 中共用同一套内容边距，避免 normal 结果页切换 skim 时因专用顶部偏移产生跳动。

## 0.8.2

### 新增

- 重绘并统一替换全部已登记文件格式图标，常见扩展名别名复用同一专用图标，未知格式继续使用通用文件图标；另新增 3DM、ISO、SWF、GGUF 与 SAFETENSORS 浏览、通用文件搜索和文件信息支持，其中两种模型权重格式共用模型类别与图标，不进入正式缩略图或 AI 识别范围。
- 全部 123 种已登记格式统一进入已添加目录的通用文件扫描，可按文件名、扩展名、目录路径和手工关键词搜索，并可在搜索结果中编辑关键词或删除；其中仅原有 15 种视觉格式继续生成正式缩略图并进入图片识别、AI 与图片统计。
- 搜索结果新增与 skim 共用的“精简查看 / 全部查看 / 自定义查看”范围标签；两种视图分别保存当前模式并共享一份自定义格式列表，默认精简范围收敛为 55 种常见成品、办公、媒体和创作项目格式。
- 正式搜索、skim 与独立预览窗口的右键菜单改为“查看 / 操作”两级结构；一级面板顶部以纯文字紧凑显示格式、居中省略的文件名及不带字段名称的尺寸、大小等结果值，弹出时按最终双列宽度预留位置，悬停入口后同一菜单固定向右展开第二列，在 micro 中保留完整动作空间且不移动一级内容，并恢复单选及多选逐行复制绝对路径。
- 取消普通单击缩略图后另行弹出的文件信息卡，普通单击仅负责选择；skim 文件夹摘要在右键菜单打开后延迟统计总大小与后代数量，菜单关闭时取消未完成统计，常见栅格图片仍只读取受限尺寸元数据。
- 正式搜索中的非视觉文件补齐编辑关键词与删除文件操作；手工关键词独立保存在通用文件目录层，可与视觉文件混合批量编辑并参与现有精确关键词搜索，不进入图片识别、AI、OCR 或图片统计，删除仍经过已添加目录、受保护路径和回收站校验。
- 新增 EPUB 安全内容预览：显示封面、书名、作者和连续垂直纯文本章节，不执行书内脚本、CSS 或网络资源。
- 新增未加密 MOBI6 安全内容预览：显示封面、书名、作者和连续垂直纯文本章节；不执行书内脚本或网络资源，不支持 DRM、KF8、AZW/AZW3 与 HUFF/CDIC 压缩。
- 正式搜索与 skim 新增 CSS、JavaScript、Python 源码式只读预览；复用 1 MiB、UTF-8/UTF-16 与非法二进制回退边界，不执行源码、不解析依赖，也不建立正文索引。

- skim 对当前可见的非原生视觉文件按需请求 Windows Shell 或已安装第三方缩略图处理器；成功结果写入独立 skim 缓存，无法生成时静默回退格式图标，不扩大正式索引、搜索或 AI 识别范围。
- 正式搜索与 skim 的共用预览窗口新增 INI、HTML、CSV、JSON、XML、YAML 和 YML 源码式只读预览；保留 1 MB 上限与 UTF-8/UTF-16 编码边界，HTML 不执行，读取失败静默回退文件信息。
- 经 Electron/Chromium 真实样本解码验证后，正式搜索与 skim 的共用预览窗口新增 M4A 音频和 WEBM 视频播放，继续复用当前文件授权、Range 串流、媒体释放与文件信息回退边界。
- 经 Electron 43 / Chromium 150 真实样本验证后，正式搜索与 skim 的共用预览窗口新增 FLAC、OGG 音频和 MKV 视频播放；AVI 样本无法取得元数据或解码，继续回退文件信息。
- 正式搜索与 skim 的共用预览窗口新增 PDF 多页连续纵向预览；仅加载视口及邻近页面，远离页面释放图片，支持页码跳转、文档滚动和原有左右方向键切换文件，损坏或无法解析的 PDF 静默回退文件信息。
- 正式搜索与 skim 新增 Office 文件基础预览：DOC/DOCX 安全提取正文文本，XLS/XLSX 与 PPT/PPTX 在已安装 Microsoft Excel/PowerPoint 时只读转换为会话临时 PDF 并复用连续页面预览；转换失败、超时或缺少对应 Office 组件时静默回退文件信息，不提供版式优化、编辑或内容搜索。
- AVIF 从 skim browse-only 格式提升为正式视觉格式，进入目录扫描、正式搜索、缩略图与预览缓存及 AI 模型输入；继续通过 sharp/libvips 转换为受限尺寸代表图，不扩大 HEIC 或 RAW 能力。
- 正式搜索与 skim 的共用预览窗口新增 ZIP、7Z、RAR 只读条目列表；使用独立辅助进程显示中文路径、文件夹、条目大小与总量，不解压、不打开内部文件、不输入密码，加密、损坏、超时或不支持的归档回退文件信息。
- 正式搜索与 skim 的共用预览窗口新增 TTF、OTF 字体安全预览；无需安装字体即可显示本地化家族名、样式、字形数量和固定中英文示例，不支持的字符明确提示，损坏、超限或超时回退文件信息。

### 优化

- 搜索结果中的通用文件卡片与 skim 条目对长文件名统一使用中间省略，优先保留名称开头、末尾片段和扩展名，并继续通过悬停显示完整名称。
- Settings 顶部搜索胶囊与正式结果页共享同一组“查看范围、排序、目录、识别状态”标签配置和排列顺序；在 Settings 中切换查看范围会更新同一份搜索结果查看偏好。
- 移除正式搜索入口与结果页顶部的文件格式动态标签及展开筛选，避免目录格式较多时标签占满 micro / mini 空间；扩展名仍可直接作为搜索关键词使用，右键菜单继续显示当前文件格式与详细信息。
- 搜索结果页中，用户将非空关键词清除后延迟恢复当前筛选范围内的全部文件；重新输入、按回车、切换筛选或离开结果页会取消等待，快捷命令及 skim、Settings 的程序化清空不触发自动搜索。
- 搜索结果为空或尚未添加目录时，将进入 skim 与拖入文件夹添加目录两种入口合并为一行紧凑的中英文提示。
- Settings“自定义查看”的格式按钮统一限制为紧凑宽度，超过可用长度的扩展名改用保留首尾的中间省略显示，完整格式名继续通过悬停提示提供。
- 统一文件图标呈现：视觉缩略图加载失败时的占位图标改用与格式文件一致的响应式尺寸，全部专用格式、通用文件、文件夹与磁盘图标统一跟随主题色和副色渐变；紧凑的未识别文件列表继续保留 30 px 占位尺寸。
- skim 的内部滚动视口复用搜索结果页的圆角继承与绘制裁切，条目滚动到区域边缘时按统一圆角收口，不再在底部边角露出直角内容。
- 精简右键菜单中的文件夹内容摘要，以“文件数 文件 / 文件夹数 文件夹”替代较长的完整句式，为大目录的多位数量保留更多显示空间。
- 扩大 Settings“自定义查看”格式类别的交互范围，圆点与类别文字作为同一按钮响应悬停和整类切换；已选格式按钮根据【颜色2】亮度自动选择深色或浅色文字，保持自定义配色下的可读性；隐藏文件按钮保持原文案，仅通过悬停明确其只影响自定义 skim。
- 调整正式搜索结果页的顶部标签顺序；新增查看范围后按“查看范围、排序、目录、识别状态”排列，与 skim 一致保留排序入口为从左至右第二项。
- 拆分正式搜索与 skim 的排序偏好：正式搜索继续恢复用户原有选择，skim 独立持久化名称 / 修改时间与升序 / 降序组合并默认按文件名升序；在两个视图间切换或重启后不再互相覆盖。
- 正式搜索统一从通用文件目录层关联视觉识别层，支持文件名、扩展名、已添加根目录真实名称、用户显示名称与相对目录跨字段多词命中；连续查询在 15 秒内复用可取消的内存扫描快照，减少大目录重复递归扫描，同时保持正文、OCR、AI 与相关性排序边界不变。
- 更新构建工具链间接依赖中的 `fast-uri`、`brace-expansion`、`js-yaml` 与 `nanoid` 安全补丁版本，生产与完整依赖审计均恢复为 0 条漏洞。

### 修复

- 修复独立预览窗口右键菜单仅在通用文件信息预览中显示文件大小、图片及 PDF、音视频等专用预览遗漏大小摘要的问题；预览会话现在统一携带当前项目大小，文件夹仍显示后台统计的总大小。
- 修复 Settings 语言偏好为“跟随系统”时，按钮错误显示当前解析语言、造成第一次点击看似没有切换的问题；系统偏好现在重新按当前界面语言明确显示“跟随系统”或“Use System Setting”，显式中文与 English 状态保持独立。
- 修复 normal 页面及 normal 尺寸 skim 拖入文件夹时，全窗口确认层首帧被通用编辑面板宽度限制为中央竖条、随后才由背景淡入补满窗口的问题；全窗口确认层现在从出现首帧即占满内容区域，内部提示继续保留原淡入动效。
- 修复从 skim 进入 Settings 后点击 skim 图标会覆盖原始返回目标、导致 Settings 与 skim 之间循环切换的问题；底部 skim 与设置图标现在常驻高亮当前页面，从该路径点击任一图标均先返回原 skim，随后可正常退出到搜索结果页。
- 修复 APNG 文件虽然使用 `.png` 扩展名但预览只显示静态首帧的问题；主进程现在只对包含多帧 `acTL` 动画控制块的 PNG 直接交付源文件，普通 PNG 继续使用受限尺寸的静态预览缓存。
- 修复误按 `Ctrl+-` 等 Chromium 页面缩放快捷键后整个界面被缩得很小且难以恢复的问题；主窗口、预览窗口与启动提示窗口现在载入时统一恢复 100% 页面比例，并拦截键盘与滚轮页面缩放，不影响软件自身的网格尺寸设置。
- 修复首次启动后立即搜索或通过快捷键进入结果页时，内容视图激活通知与扫描快照发生竞态，导致结果中央显示英文 `Search scan cancelled` 异常的问题；正常生命周期取消现在回退现有索引，真实搜索失败也只显示当前界面语言的通用提示。
- 修复部分企业 Windows 环境中 skim 打开磁盘、进入目录和返回上级时，因反复启动 PowerShell 并触发安全扫描而产生的固定延迟；磁盘枚举、卷标和隐藏属性读取改用轻量 Windows 系统命令，保留盘符名称与隐藏文件过滤。

## 0.8.1

### 新增

- Settings 新增用户主动触发的版本检查：发现更高版本后先显示版本号并将按钮切换为“立即下载”，再次点击才通过系统浏览器直接下载受限命名的 Windows x64 多文件 ZIP；软件不后台检查、不自动写入或覆盖安装。
- 新增可关闭的 Windows 系统通知：首次运行提示后台常驻入口；后台 AI 识别或自动缓存优化实际持续超过 1 分钟后发送单次完成结果，并为缓存通知增加批次去重与冷却，减少打扰。
- skim 顶部新增持久化浏览模式标签：默认 skim、全部显示与自定义；Settings 提供格式类别、具体扩展名和 Windows 隐藏文件配置，并复用现有展开动效。

### 优化

- skim 当前一级目录支持显示全部普通文件；中央能力表之外及无扩展名文件使用通用图标和文件信息预览，不因此进入正式索引、缩略图生成或 AI 识别。
- llama.cpp 与视觉模型下拉列表的首项支持取消当前选择并持久化为“未选择”；重新选择时同步清除旧失败提示，非空选项继续验证实际文件，服务运行期间仍禁止切换。
- Settings 语言入口使用“语言 / Language”双语标签，英文界面交换显示顺序；跟随系统时显示当前实际语言，同时继续保留系统跟随偏好。
- 补齐 Settings 各区域按钮的悬停说明，按点击、展开、切换和状态操作统一中英文提示；短设置项的宽度和 skim 自定义配置布局同步整理。
- 保留 line 连续渐变动画，将 Windows 原生最小高度造成的透明待机窗口裁成底部 15 px 有效区域；line 与 capsule 在任务栏自动隐藏导致工作区变化后重新贴合底部。
- 降低后台空闲资源占用：避免启动和目录列表加载隐式递归扫描，延后缓存统计与模型配置读取；自动缩略图优化仅在主窗口聚焦且查看内容时领取任务，搜索扫描支持协作取消。
- standby 与 capsule 的鼠标穿透检测改为自适应调度；鼠标静止且远离窗口时降低轮询频率，进入交互范围后恢复响应速度。
- 收起到 standby 或 capsule 后卸载搜索结果网格及图片节点但保留结果元数据；独立预览窗口关闭后短期复用，持续空闲 2 分钟后销毁 Renderer。
- 调整 skim 顶部标签顺序，使浏览模式、排序和面包屑依次显示，其余交互保持不变。

### 修复

- 修复快速关闭并复用预览窗口时，上一轮延迟隐藏事件可能清空新预览内容并形成透明空窗口的问题；旧内容改由主进程关闭会话时显式重置。
- 修复从 skim 快速退出时，未完成的异步空格预览可能在搜索结果页重新打开预览并隐藏主窗口的问题。
- 修复关闭并快速复用独立预览窗口时，上一项目内容可能在当前预览出现前短暂闪现的问题；窗口隐藏后立即卸载旧内容和媒体状态。

## 0.8.0

### 新增

- 多文件打包内容在 `Cap7CE.exe` 同级预置空的 `llama.cpp` 与 `models` 目录，用户可直接放入运行时和模型文件。
- 新增 skim 本地文件浏览模式：支持磁盘与目录逐级浏览、路径标签、排序与格式筛选、文件和文件夹信息预览、右键操作、多选及原生拖出；skim 复用 micro、mini、normal 与预览窗口的现有壳层和控制组件，不新增主窗口状态。
- 建立中央格式能力表和通用文件目录层，将项目关联的文本、音频、视频、字体、压缩包、办公文档及三维文件等非视觉格式纳入 skim 与已添加目录的文件名搜索，同时继续保持视觉索引和 AI 识别边界。
- skim 快速预览支持 TXT/MD 纯文本、MP3/WAV 音频、MP4/MOV 视频、动态 GIF/WEBP 及现有视觉格式；音视频默认播放并循环，竖版视频按内容比例适配窗口。
- 新增独立 skim 缩略图、预览图与元数据缓存，按可见区域和预览优先级生成，可在 Settings 单独统计和安全清理，不影响正式视觉缓存。
- 支持从资源管理器向 micro、mini、normal 和 Settings 拖入一个或多个文件夹，经共用确认层加入搜索目录；skim 中的文件和文件夹均可拖出至外部应用。
- 快捷动作新增“进入 skim”，默认使用 `Alt+5`，打开 Settings 调整为 `Alt+6`；快捷指令新增 `skim:`、`skim:root` 与 `cache:skim`。

### 优化

- skim 网格改为虚拟化渲染，大量文件下调整窗口尺寸时只计算并渲染可见区域，显著降低卡顿。
- 统一 skim 与搜索结果页的顶部胶囊、路径层级、标签折叠、排序入口、网格尺寸、间距、文件图标和次级文字；micro、mini、normal 下保持响应式布局与滚动能力。
- 正式搜索与 skim 共用预览窗口、窗口控制栏和自绘滚动条；预览窗口可直接进入或退出 skim，文本预览滚动不再误切换文件。
- Settings 的配置目录和索引状态复用快捷动作/快捷指令的展开收起动效，并为配置目录补充管理/收起入口。
- 统一 skim 图标、命名、按钮提示和返回关系；从 line 或 capsule 进入 skim 后退出回到 normal，从 Settings 进入则返回 Settings。
- 更新 electron-builder 间接依赖中的 `brace-expansion` 修复版本，生产与完整依赖审计均保持 0 条漏洞。

### 修复

- 修复 skim 在大量内容下网格重叠、文件名显示不完整、缩略图焦点描边残留，以及 micro 横向滚动和 mini/normal 布局异常的问题。
- 修复 skim 鼠标侧键只能后退不能前进、路径层级颜色和当前目录名称不一致、中文磁盘卷标乱码的问题。
- 修复首次运行直接进入 skim 后退出时结果页未初始化而显示空内容的问题。
- 修复 skim 内部拖出的文件夹重新释放到应用时误触发添加目录确认的问题。
- 修复非视觉文件图标在 skim 和正式搜索中的尺寸、基线与居中表现不一致的问题。

## 0.7.8

### 新增

- Settings 的主题色与副色改用主题感知的自绘拾色器，提供明度 / 饱和度区域、色相滑块和 HEX 输入；支持带或不带 `#` 的逐字符输入、界面实时预览、点击外部保存，以及按 `Esc` 取消并恢复原颜色。

### 优化

- Electron 从 31.7.7 升级至 43.2.0，同时更新 Vite、electron-builder、concurrently 与 Node.js 类型定义等开发工具链依赖。
- 清除开发工具链中由 `shell-quote` 引起的 Critical 告警，继续保持生产依赖审计为 0 条漏洞；未使用 `npm audit fix --force`。
- 拾色器复用右键菜单的明亮 / 黑暗主题、圆角和弹出动效，HEX 文本框背景跟随当前颜色，并根据亮度自动切换文字颜色。

### 修复

- 修复 Electron 43 在 Windows 上对极窄待机窗口应用原生最小高度后，line 被挤出工作区底部而不可见的问题；现在按实际窗口高度重新贴合工作区底边。
- 修复使用空格打开预览、滚动切图并再次按空格关闭后，起始缩略图与结束缩略图同时呈现选中状态的问题。

## 0.7.7

### 新增

- Settings 底部版本号可在系统默认浏览器中打开 Cap7CE 的 GitHub Releases 页面，使用固定主进程链接且不执行自动联网检查。
- 快捷指令新增 `app:startup on / off` 与 `app:hints on / off`，可直接控制开机运行和搜索框操作提示，并在 Settings 快捷指令列表中统一展示。

### 优化

- AI 识别任务按启动时已解析的软件语言生成中文或英文描述与关键词；跟随系统模式使用系统解析结果，任务运行期间切换语言不会造成同一批结果中英混杂。
- Settings 的 llama.cpp 版本与视觉模型选择改为自绘下拉菜单：使用与按钮等高的无描边触发框，移除原生箭头和选中描边，复用右键菜单的主题色 hover、圆角与左侧移入动效，并保留键盘选择、禁用与视口定位行为。
- 缓存管理统一统计并清理缩略图、模型输入图和预览图三类视觉缓存，显示三类缓存图像条目与元数据的实际总大小；执行清理后仍按现有逻辑关闭自动优化。
- 自动缓存优化不再在每次启动后独立全量扫描目录；改为复用正常目录扫描结果和内存缓存清单，只为新增、修改、缺失或内部版本失效的缩略图排队生成。
- AI 识别成功并写入索引后自动回收对应的模型输入缓存；失败或取消项目继续保留以便重试，启用自动缓存优化时同步清理历史已识别项目遗留的模型输入缓存。
- 整理 Settings【详细信息】中的运行时与模型诊断字段：修正文件状态和服务地址语义，服务运行时补充 PID 与启动时间，区分可用模型组合数和 GGUF 文件数，并允许选中复制详情值；中英文文案保持同步。
- 缩略图悬停时仅放大内部图片，选中后固定保持放大并沿用原透明背景；缩略图容器尺寸与位置保持不变，避免白边、漏底和裁剪抖动。
- 预览换图不再叠加前后两张图片；新图未就绪时隐藏旧图，并仅在加载超过短暂延迟后显示等待提示，已缓存图片切换不再闪出等待状态。
- 图片与顶部标签右键菜单统一选中项的主题色渐显渐隐效果，不影响菜单本身的出现动画。
- 统一搜索结果与 Settings 顶部区域间距，保持各页面内容起点一致。

### 修复

- 修复在 Settings 对当前浏览目录执行 AI 识别后，已加载结果未重新查询，导致关键词编辑仍读取识别前空数据的问题；识别完成后按原筛选与排序静默刷新当前结果。
- 修复等待动画的渐变样式影响窗口控制栏图标，导致图标出现主题色与副色渐变的问题。
- 修复从 Settings 识别状态统计进入结果页后，首次点击设置图标错误跳转到全部文件、需要再次点击才能返回 Settings 的问题。
- 修复新增目录完成扫描后只更新目录行数量、未同步写入待识别索引，导致 Settings 顶部全部文件和未识别统计漏掉该目录的问题。
- 修复并发读取缓存文件大小时累计值互相覆盖，导致缓存管理只显示单个元数据文件大小的问题。
- 修复开发版与打包版等多个实例共享配置并争抢全局快捷键时，后启动实例因临时注册冲突而永久关闭【快捷动作】的问题；注册失败现在仅标记本次运行不可用，不再改写用户的总开关偏好。
- 修复 Settings【快捷动作】按钮显示点击动作而非当前开关状态，与其他基础行为设置语义相反的问题。
- 修复 Settings 目录名称进入双击编辑状态后，输入框高度大于原名称标签的问题。

## 0.7.6

### 优化

- 统一 `Esc` 为不可配置的窗口内取消键，按交互层级依次取消快捷键录入、快捷指令确认、菜单、标签展开、编辑、确认弹层或结果选择；无可取消内容时不切换页面、不收起窗口，也不退出软件。
- 首次搜索与重启后继续使用用户保存的排序字段和方向，不再由初始化流程覆盖当前排序偏好。
- 调整空结果提示、目录重命名输入框、运行时下拉框和快捷指令说明样式，并精简自动缓存优化的英文运行提示。
- `Alt+4` 遵循 line 显示偏好：用户关闭 line 后不再由快捷动作主动重新显示。
- 预览窗口复用主窗口主题描边与边缘吸附规则，同时保持独立窗口、内容尺寸自适应和新会话居中逻辑。
- 建立统一轻量交互动效：标签依次展开与收回、缩略图蒙版和居中放大、滚动条悬停反馈、右键菜单移入、按钮颜色过渡、全窗口弹层出现、预览换图以及 Settings 展开项使用一致的节奏与减少动态效果降级。
- 【快捷动作】【快捷指令】和【详细信息】统一为全宽后纵向展开、纵向收起后恢复半宽的稳定动效，文字与背景保持同步裁剪。
- 文件格式标签增加悬停意图判断：稳定悬停后才渐显格式名称，快速扫过缩略图时保持标签文字稳定。

### 修复

- 修复全窗口编辑、删除和缓存确认层未完整覆盖主界面控制区域，导致底层按钮仍可能响应的问题。
- 修复关闭预览后结果网格没有定位到最后浏览图片、仍停留在原滚动位置的问题。
- 减少明亮模式连续切换预览图片时出现空白窗口闪烁的问题，换图期间保留最后一张已加载图片并在新图就绪后渐显替换。

## 0.7.5

### 新增

- 新增文件格式筛选：父标签可展开当前范围内的全部格式，选择后仅显示对应格式；目录、识别状态和格式标签统一父子层级、展开与弃选规则。
- 新增排序筛选：可在升序 / 降序与按名称 / 按修改时间之间组合切换，并同步保存当前排序偏好。
- 新增 Windows 开机运行设置，按普通启动流程打开 Cap7CE，并持久化用户选择。
- 新增自动缓存优化：后台按当前排序串行生成搜索缩略图，支持持久化开关、动态统计、AI 识别期间暂停，以及清理缓存前安全停止。
- 新增可关闭的搜索框操作提示：首次显示固定指引，之后在窗口模式切换或输入重新清空时随机更新；快捷键提示读取当前用户配置并补充中英文文案。
- 托盘菜单新增打开 / 关闭边缘吸附选项，并与 Settings 状态实时同步。

### 优化

- 统一目录、识别状态、排序和文件格式标签的默认、同类展开及当前选中样式，父标签可直接弃选子项并收起。
- 将开机运行、操作提示、line 和边缘吸附归入同一基础行为设置组；颜色与快捷键配置统一为单击修改。
- 主窗口与 capsule 补充主题边框，并统一明亮 / 黑暗模式下窗口控制按钮和状态文字颜色。
- 缓存扩展名由旧产品前缀统一为 `.capth`、`.capmo` 和 `.cappr`，分别对应缩略图、模型输入图和预览图。

### 修复

- 修复 Settings 页面无法执行快捷指令的问题。
- 修复 `Ctrl+\`` 在部分窗口模式下无法将焦点移回搜索框的问题。
- 合并文件格式筛选中的同义扩展名：JPG / JPEG 与 TIF / TIFF 各自使用同一标签和筛选结果。

## 0.7.4

### 新增

- 新增中文、English 与跟随系统三种界面语言模式；语言偏好持久化并同步应用于主窗口、独立预览窗口和托盘菜单。
- 建立稳定文案 ID、中英文语言表及键名和占位符一致性检查，快捷指令 `lang:auto`、`lang:cn`、`lang:en` 正式接入语言切换。

### 优化

- 统一当前中文界面的模式、主题、排序、识别、设置和快捷指令术语，并将界面可见文字集中到共享语言模块。
- 新的预览会话重新回到当前屏幕中央，同时保留同一预览会话内的文件切换和尺寸自适应逻辑。
- 缩短目录重命名快捷指令示例，固定文件格式标签的视觉宽度和留白。

### 修复

- 修复通过快捷动作直接进入 mini 后，窗口宽度可被缩小到低于统一最小尺寸的问题。
- 修复鼠标经过或选中不同长度的文件格式时，顶部搜索胶囊换行并带动结果区域抖动的问题。

## 0.7.3

### 新增

- 新增统一等待状态：大型文件首次预览时先显示预览窗口和旋转等待图标，批量删除时显示正在执行反馈。
- 新增全局快捷键实际可用性检测；不可用或内部重复的配置在对应项目内显示提示，失败配置不写入偏好。
- 快捷键录入期间临时暂停 Cap7CE 全局快捷键，避免已有动作抢先触发；完成、取消或离开配置页后自动恢复。
- 补充 `win:max`、`win:top on / off` 和 `dir:rename` 快捷指令执行能力。

### 优化

- 统一可拉伸窗口的最小尺寸边界，并使默认位置下的 micro 横向缩放保持以窗口中心向两侧展开。
- 预览窗口在滚轮或方向键切换文件后，根据新内容尺寸重新自适应；最大化期间记录最新尺寸并在恢复时应用。
- 快捷指令成功、失败、缺少参数和二次确认提示统一为输入框内纯文字反馈；二次确认改为输入 `y / n` 后回车。
- 快捷指令查看页分组标题取消加粗，未接入指令保留并明确提示暂不支持。
- 目录和识别状态标签展开后以主题色标识同类项目，收起或完成选择后恢复默认样式。
- 自绘滚动条仅在滚动、拖动或悬停时显示，操作结束后自动淡出，但保留原有滚动槽和交互范围。

### 修复

- 修复托盘隐藏待机线后通过快捷动作进入 Settings 只出现任务栏图标，以及后续窗口切换显示错误或无法交互的问题。
- 修复 micro / mini 被拉伸到只剩按钮或标签，以及不同入口进入 micro 时中心缩放规则不一致的问题。
- 修复预览窗口切换文件后始终保持首次打开尺寸的问题。
- 修复大型文件首次预览时主界面短暂消失、批量删除期间缺少反馈而看似卡死的问题。
- 修复录入与现有 Cap7CE 快捷键相同的组合时，旧全局动作先于录入流程触发的问题。

## 0.7.2

### 新增

- 新增未识别、解析失败和文件错误的紧凑响应式卡片视图，以及缩略图渐变占位兜底。
- 新增识别状态标签，可在全部文件、已识别和未识别结果间直接切换。
- 新增可交互自绘滚动条，支持纵向与 micro 横向 thumb 拖动和轨道点击。
- 预览窗口接入统一窗口控制栏，支持缩放、最大化、共享置顶状态和右侧空白拖动。
- 托盘图标双击可直接进入 normal 并显示全部已添加文件。
- 新增激活 micro、mini、normal、standby 和 Settings 的可配置全局快捷动作。

### 优化

- capsule、micro、mini、normal 和 Settings 统一主窗口壳层、右侧控制栏、按钮顺序及 Electron 原生 drag / no-drag 规则。
- 未执行 AI 识别的已扫描文件也可直接通过文件名搜索命中。
- 目录、识别状态、排序和文件格式标签默认全部显示，并分别保存显隐偏好；默认排序改为递减。
- 统一标签与缩略图右键菜单的视觉样式、圆角和窗口内定位。
- 统一主题配置、快捷动作、Settings 按钮和窗口控制按钮的双击提示与键盘焦点反馈。
- 普通结果、未识别结果和透明图片复用统一缩略图背景、加载失败占位及选中反馈。
- 调整主内容、滚动条槽和窗口控制栏的边缘留白，保持各窗口模式视觉一致。

### 修复

- 修复 capsule 内容裁剪、控制栏拖动失效以及旧滚动层覆盖窗口拖动区的问题。
- 修复预览窗口与主窗口置顶状态不同步、置顶图标状态滞后和切换置顶时主窗口意外显示的问题。
- 修复 normal 最大化后经快捷指令切换 mini 再返回时窗口圆角变为直角的问题。
- 修复 Settings 未添加目录时清理缓存区域间距异常，以及部分设置项交互方式和焦点描边不一致的问题。
- 修复 micro 未识别结果无法使用鼠标滚轮滚动及卡片显示不完整的问题。
- 修复托盘首次双击进入 normal 时没有加载全部内容的问题。

## 0.7.1

### 新增

- 支持多选批量编辑关键词，并以事务保证整批提交或回滚。
- 支持未识别文件手动添加关键词并立即参与搜索。
- llama-server 自动选择可用本地端口。
- 新增独立图片预览窗口。

### 优化

- 统一主窗口、预览窗口、预览图与搜索结果滚动区域圆角。
- 缩略图 Hover / Selected 状态改为黑色透明蒙版。
- Settings 索引状态改为行内展开显示。
- 新增目录后在默认搜索范围内自动显示全部支持文件。
- 关键词解析统一为英文逗号规范格式。
- 普通界面文字默认不可选中，输入区域保持正常文本选择。
- 副色按钮交互效果同步当前副色。

### 修复

- 修复手动关键词保存后无法搜索的问题。
- 修复未识别文件批量编辑关键词失败的问题。
- 修复固定端口被占用或被 Windows 排除时 llama-server 无法启动的问题。
- 修复删除目录后 Settings 内容空白的问题。
- 修复新增首个目录后搜索结果为空的问题。
- 修复编辑关键词后搜索结果滚动位置重置的问题。
- 修复透明窗口圆角处的半透明阴影残影。
- 修复缩略图状态反馈不清晰的问题。
- 移除目录、索引、删除结果和缓存结果使用的旧版 Modal 状态提示。
- 修复搜索结果中的真实文件因未命中有效索引记录而无法移入回收站的问题。
- 文件删除权限现以“位于已添加目录内的受支持真实文件”为边界，不再依赖数据库记录或识别状态。
