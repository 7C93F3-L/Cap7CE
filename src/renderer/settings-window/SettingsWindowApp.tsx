import "./SettingsWindowApp.css";

const SettingsWindowApp = () => (
  <div className="cap-settings-window-foundation">
    <div className="cap-settings-window-drag-region" aria-hidden="true" />
    <main className="cap-settings-window-placeholder">
      <span>Stable UI · U6</span>
      <h1>Settings window foundation</h1>
      <p>独立窗口宿主已接入；正式设置内容将在下一轮迁移。</p>
    </main>
  </div>
);

export default SettingsWindowApp;
