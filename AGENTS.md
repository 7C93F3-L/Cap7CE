# Cap7CE 项目规则

## 开始工作

- 修改前先阅读 `CHANGELOG.md` 和 `docs/SOFTWARE_ARCHITECTURE.md`。
- 先检查 Git 状态，保留用户已有及无关修改。
- 涉及窗口状态机、缓存、索引、SQLite 或跨进程功能时，先讨论方案。
- 明确且局部的界面调整可以直接实施。

## 修改范围

- 优先进行最小范围修改，不顺手重构无关代码。
- 保持现有 micro、mini、normal、line 和 Settings 状态语义。
- 不恢复已经移除的 edge hidden 行为。
- 不修改用户数据和旧缓存，除非任务明确要求。

## 模块边界

- `src/renderer/App.tsx` 只承担应用级状态组合、页面导航和顶层交互编排；不得继续新增完整页面、大型设置区块、网格、弹层或 Provider 专用界面。
- 新增 Renderer 功能必须进入职责明确的组件、Hook、控制器或领域目录；纯展示组件不得依赖应用级状态，跨视图共享类型放入 `src/shared`，不得复制相似类型。
- `electron/main.ts` 只承担应用启动、BrowserWindow / Tray 生命周期、窗口状态机和顶层服务装配；除窗口与预览生命周期外，新增 IPC 必须进入对应领域的注册模块。
- 不得复制或旁路现有窗口切换、显示恢复、preview、line、capsule、skim 导航链路；新入口必须复用统一动作。
- `src/renderer/styles.css` 保留现有全局变量、壳层和兼容规则；新增独立组件的样式应进入对应领域样式文件，不得默认继续堆入全局样式入口。
- 未经独立架构任务批准，不得使 `App.tsx`、`electron/main.ts` 或 `styles.css` 超过当前整理基线；完成拆分后应同步降低自动检查上限。
- 只为缩短文件而转移代码不算完成：新模块必须具有单一职责、明确依赖方向和可单独验证的边界。

## 架构整理

- 架构整理与用户可见功能、文案、视觉调整分开提交；纯搬迁阶段不得顺手改变行为。
- 优先拆纯函数、纯展示组件和外围 IPC；React Effect、共享状态、窗口状态机和预览生命周期必须最后处理，并先补足对应验证。
- 每轮只拆一个职责域，完成构建、完整测试和对应人工回归后才能进入下一轮。
- 新增或调整模块边界时同步更新 `docs/SOFTWARE_ARCHITECTURE.md`；不得只改代码而留下过时架构说明。
- 架构约束应由自动检查守护，而不只依赖文档；违反边界或体量上限时测试必须失败。

## 验证

- 完成代码修改后运行 `npm run build`。
- 运行 `git diff --check`。
- 交互和视觉效果由用户手动验证。
- 未执行 `npm run pack` 时，不得声称安装包验证通过。

## Git

- 用户确认后再提交。
- 每个独立功能或修复使用独立提交，除非用户明确要求覆盖提交。
- 不提交临时测试文件、计划草稿或动效实验页面，除非用户明确要求。
- 个人项目、沟通记录和未实施规划保存在公开仓库之外。
- 不使用 `git reset --hard` 等破坏性操作。

## 发布

版本发布时同步检查：

- `package.json`
- `package-lock.json`
- `src/renderer/App.tsx` 中的软件版本
- `CHANGELOG.md`
- `docs/SOFTWARE_ARCHITECTURE.md`
